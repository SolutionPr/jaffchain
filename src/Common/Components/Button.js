import React from 'react'

const Button = ({ text }) => {
    return (
        <>
            <button className="botton-custom zoom-effets">
                {text}
            </button>
        </>
    )
}
export default Button
